# Проверка на четность (битовые операции)
Language: javascript
Reward: 30

Напишите функцию `isEven(num)`, которая принимает целое число и возвращает `true`, если число четное, и `false`, если нечетное.
**Условие:** Для решения задачи необходимо использовать поразрядный оператор `&` (AND).

## Starter Code
```javascript
function isEven(num) {
    // ваш код
}
```
